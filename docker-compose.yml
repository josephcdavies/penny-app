services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "80:3001"
    env_file: .env
    environment:
      - PORT=3001
    volumes:
      - uploads:/app/server/uploads
      - db:/app/server/db
    restart: unless-stopped

volumes:
  uploads:
  db:
